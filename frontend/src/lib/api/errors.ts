import axios from "axios";
export class ApiClientError extends Error { constructor(message: string, public status?: number, public code?: string, public details: Record<string, unknown> = {}) { super(message); this.name = "ApiClientError"; } }
export function normalizeApiError(error: unknown) { if (!axios.isAxiosError(error)) return error instanceof ApiClientError ? error : new ApiClientError("Something went wrong. Please try again."); const body = error.response?.data as { message?: string; data?: { code?: string; details?: Record<string, unknown> } } | undefined; if (!error.response) return new ApiClientError(error.code === "ECONNABORTED" ? "The request timed out. Please try again." : "We could not reach TaxPilot. Check your connection and try again."); return new ApiClientError(body?.message || "The request could not be completed.", error.response.status, body?.data?.code, body?.data?.details); }
