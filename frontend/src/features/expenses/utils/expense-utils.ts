import type{ExpenseCategory,ExpenseStatus}from"../types/expenses.types";export const categories:ExpenseCategory[]=["travel","office","rent","utilities","marketing","salary","professional_fees","software","hardware","other"];export const statuses:ExpenseStatus[]=["draft","approved","paid","cancelled"];export const expenseSorts=["created_at","-created_at","expense_date","-expense_date","expense_number","-expense_number","status","-status","category","-category","total_amount","-total_amount"];export const vendorSorts=["created_at","-created_at","name","-name","vendor_code","-vendor_code","email","-email","is_active","-is_active"];export const page=(v:string|null)=>Number.isInteger(Number(v))&&Number(v)>0?Number(v):1;export const size=(v:string|null)=>[10,20,50,100].includes(Number(v))?Number(v):20;export const sort=(v:string|null,a:string[],f:string)=>v&&a.includes(v)?v:f;export const status=(v:string|null)=>statuses.includes(v as ExpenseStatus)?v as ExpenseStatus:undefined;export const actions=(s:ExpenseStatus):Array<"approve"|"pay"|"cancel">=>s==="draft"?["approve","cancel"]:s==="approved"?["pay","cancel"]:[];export const title=(v:string)=>v.replaceAll("_"," ").replace(/\b\w/g,l=>l.toUpperCase());export const money=(v:string|number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR"}).format(Number(v));export const date=(v:string|null)=>v?new Intl.DateTimeFormat("en-IN",{dateStyle:"medium"}).format(new Date(v)):"Unavailable";
