import{z}from"zod";export const registrationSchema=z.object({gstin:z.string().trim().regex(/^\d{2}[A-Z]{5}\d{4}[A-Z][A-Z\d]Z[A-Z\d]$/,"Enter a valid GSTIN"),legal_name:z.string().trim().min(3).max(150),trade_name:z.string().nullable(),registration_type:z.enum(["regular","composition","casual","non_resident"]),state_code:z.string().regex(/^\d{2}$/),registration_date:z.string().min(1),is_composition_scheme:z.boolean(),is_active:z.boolean()});export type RegistrationValues=z.infer<typeof registrationSchema>;export const settingsSchema=z.object({default_tax_mode:z.enum(["intra_state","inter_state"]),tax_inclusive:z.boolean(),rounding_method:z.enum(["nearest","up","down"]),allow_manual_override:z.boolean()});export type SettingsValues=z.infer<typeof settingsSchema>;
