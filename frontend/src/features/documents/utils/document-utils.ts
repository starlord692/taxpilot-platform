import type{DocumentStatus,DocumentType}from"../types/documents.types";export const documentTypes=["unknown","sales_invoice","purchase_invoice","expense_receipt","bank_statement","gst_certificate","pan","other"]as const;export const documentStatuses=["uploaded","ocr_pending","ocr_running","ocr_completed","ocr_failed"]as const;export const page=(v:string|null)=>Math.max(1,Number(v)||1);export const size=(v:string|null)=>[10,20,50,100].includes(Number(v))?Number(v):20;export const type=(v:string|null):DocumentType|undefined=>documentTypes.includes(v as DocumentType)?v as DocumentType:undefined;export const status=(v:string|null):DocumentStatus|undefined=>documentStatuses.includes(v as DocumentStatus)?v as DocumentStatus:undefined;export const bytes=(n:number)=>n<1024?`${n} B`:n<1048576?`${(n/1024).toFixed(1)} KB`:`${(n/1048576).toFixed(1)} MB`;export const confidence=(v:string|number)=>Math.round(Number(v)*100);export const title=(v:string)=>v.replaceAll("_"," ").replace(/\b\w/g,c=>c.toUpperCase());
