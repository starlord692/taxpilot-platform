import { describe, expect, it } from "vitest";
import { formatDate, parsePage, parsePageSize, statusTone, titleCase } from "./sales-format";
describe("sales state utilities",()=>{it("sanitizes pagination without creating API parameters",()=>{expect(parsePage("-2")).toBe(1);expect(parsePage("3")).toBe(3);expect(parsePageSize("25")).toBe(20);expect(parsePageSize("50")).toBe(50)});it("formats canonical lifecycle labels",()=>{expect(titleCase("partially_paid")).toBe("Partially Paid");expect(statusTone("paid")).toBe("success");expect(statusTone("cancelled")).toBe("danger")});it("handles missing dates honestly",()=>{expect(formatDate(null)).toBe("Unavailable")})});
