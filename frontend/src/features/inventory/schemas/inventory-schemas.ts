import{z}from"zod";export const productSchema=z.object({sku:z.string().trim().min(1).max(100),name:z.string().trim().min(1).max(255),description:z.string().nullable(),category:z.string().nullable(),unit_of_measure:z.string().trim().min(1).max(50),barcode:z.string().nullable(),purchase_price:z.number().min(0),selling_price:z.number().min(0),reorder_level:z.number().min(0),is_active:z.boolean()});export type ProductValues=z.infer<typeof productSchema>;export const warehouseSchema=z.object({code:z.string().trim().min(1).max(50),name:z.string().trim().min(1).max(255),address:z.string().nullable(),is_default:z.boolean(),is_active:z.boolean()});export type WarehouseValues=z.infer<typeof warehouseSchema>;
