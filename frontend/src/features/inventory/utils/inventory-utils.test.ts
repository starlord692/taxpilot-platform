import{describe,expect,it}from"vitest";import{active,page,productSorts,size,sort,warehouseSorts}from"./inventory-utils";describe("inventory URL utilities",()=>{it("sanitizes pagination",()=>{expect(page("0")).toBe(1);expect(page("3")).toBe(3);expect(size("25")).toBe(25);expect(size("17")).toBe(25)});it("accepts only backend sort allowlists",()=>{expect(sort("-sku",productSorts,"-created_at")).toBe("-sku");expect(sort("price",productSorts,"-created_at")).toBe("-created_at");expect(sort("code",warehouseSorts,"-created_at")).toBe("code")});it("parses only explicit activity filters",()=>{expect(active("true")).toBe(true);expect(active("false")).toBe(false);expect(active("yes")).toBeUndefined()})});
