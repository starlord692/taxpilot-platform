export const productSorts=["created_at","-created_at","sku","-sku","name","-name"]as const;export const warehouseSorts=["created_at","-created_at","code","-code","name","-name"]as const;export const page=(v:string|null)=>Math.max(1,Number(v)||1);export const size=(v:string|null)=>[10,25,50,100].includes(Number(v))?Number(v):25;export const sort=(v:string|null,a:readonly string[],f:string)=>v&&a.includes(v)?v:f;export const active=(v:string|null)=>v==="true"?true:v==="false"?false:undefined;export const quantity=(v:string|number)=>new Intl.NumberFormat().format(Number(v));export const money=(v:string|number)=>new Intl.NumberFormat(undefined,{style:"currency",currency:"INR"}).format(Number(v));
