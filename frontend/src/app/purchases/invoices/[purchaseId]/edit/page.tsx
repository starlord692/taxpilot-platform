import { PurchaseForm } from "@/components/purchases/purchase-form"; export default async function Page({ params }: { params: Promise<{ purchaseId: string }> }) { return <PurchaseForm id={(await params).purchaseId} />; }
