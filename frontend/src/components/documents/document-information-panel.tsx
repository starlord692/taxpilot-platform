import { FileText } from "lucide-react";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Unavailable } from "./document-states";
import { confidence } from "@/features/documents/utils/document-utils";
import type { Document } from "@/features/documents/types/documents.types";

export function DocumentInformationPanel({ document }: { document: Document }) { return <Card aria-labelledby="document-information-title"><CardHeader><CardTitle id="document-information-title">Document information</CardTitle><CardDescription>What TaxPilot can read from the uploaded file.</CardDescription></CardHeader><CardContent className="space-y-4"><Unavailable title="Original preview unavailable" description="Your file is stored securely, but the backend does not yet provide a protected preview link." />{document.ocr_results.length ? <div className="space-y-3">{document.ocr_results.map(result => <article key={result.id} className="rounded-xl border bg-background p-4"><div className="mb-3 flex items-center justify-between gap-3"><span className="flex items-center gap-2 text-sm font-medium"><FileText className="size-4" />Page {result.page_number}</span><span className="text-xs text-muted-foreground">Reading confidence {confidence(result.confidence_score)}%</span></div><pre className="max-h-80 overflow-auto whitespace-pre-wrap font-sans text-sm leading-6 text-muted-foreground">{result.raw_text}</pre></article>)}</div> : <p className="rounded-xl border border-dashed p-4 text-sm text-muted-foreground">TaxPilot has not read this document yet.</p>}</CardContent></Card>; }
