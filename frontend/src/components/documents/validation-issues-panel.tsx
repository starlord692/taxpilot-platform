import { AlertCircle, Info, TriangleAlert } from "lucide-react";
import { cn } from "@/lib/utils";
import { title } from "@/features/documents/utils/document-utils";
import type { ValidationIssue } from "@/features/documents/types/documents.types";

export function ValidationIssuesPanel({ issues }: { issues: ValidationIssue[] }) { const open = issues.filter(issue => !issue.resolved); if (!open.length) return <div className="rounded-xl border border-emerald-500/20 bg-emerald-500/5 p-4 text-sm text-emerald-700 dark:text-emerald-400">No unresolved information issues.</div>; return <section aria-labelledby="validation-issues-title" className="space-y-2"><h3 id="validation-issues-title" className="text-sm font-semibold">Information to check</h3>{open.map(issue => { const Icon = issue.severity === "error" ? AlertCircle : issue.severity === "warning" ? TriangleAlert : Info; return <div key={issue.id} role={issue.severity === "error" ? "alert" : undefined} className={cn("flex gap-3 rounded-xl border p-3 text-sm", issue.severity === "error" && "border-destructive/20 bg-destructive/5", issue.severity === "warning" && "border-amber-500/20 bg-amber-500/5")}><Icon className="mt-0.5 size-4 shrink-0" aria-hidden="true" /><div><p className="font-medium">{title(issue.field_name)}</p><p className="mt-0.5 text-xs leading-5 text-muted-foreground">{issue.message}</p></div></div>; })}</section>; }
