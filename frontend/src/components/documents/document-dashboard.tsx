"use client";import Link from"next/link";import{FileCheck2,Files,Upload}from"lucide-react";import{Button}from"@/components/ui/button";import{useBusiness}from"@/contexts/business-context";import{useDocuments}from"@/features/documents/hooks/use-documents";import{DocumentError,DocumentLoading}from"./document-states";export function DocumentDashboard(){const{activeBusinessId:b}=useBusiness(),docs=useDocuments(b??"",{page:1,pageSize:20});if(docs.isLoading)return <DocumentLoading/>;if(docs.isError)return <DocumentError retry={()=>void docs.refetch()}/>;return <div className="space-y-6"><div className="grid gap-4 md:grid-cols-2"><Link href="/documents/list" className="rounded-xl border p-5"><Files className="size-5 text-primary"/><p className="mt-4 text-sm text-muted-foreground">Uploaded documents</p><strong className="text-2xl">{docs.data?.meta.total??0}</strong></Link><Link href="/documents/review" className="rounded-xl border p-5"><FileCheck2 className="size-5 text-primary"/><p className="mt-4 font-medium">Pending review workspace</p><p className="text-sm text-muted-foreground">Uses the verified pending-review API.</p></Link></div><section className="rounded-xl border p-5"><h2 className="font-semibold">Quick actions</h2><Button asChild className="mt-4"><Link href="/documents/upload"><Upload/>Upload document</Link></Button></section><section className="rounded-xl border border-dashed p-5"><h2 className="font-semibold">Availability</h2><p className="mt-2 text-sm text-muted-foreground">Document totals are backend-reported. Date analytics, processing trends, and automation aggregates are unavailable.</p></section></div>}
