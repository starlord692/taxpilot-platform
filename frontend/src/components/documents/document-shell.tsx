"use client";import Link from"next/link";import{usePathname}from"next/navigation";import{FileSearch,Files,LayoutDashboard,Upload}from"lucide-react";import{BusinessRouteShell}from"@/components/business/business-route-shell";import{BusinessSwitcher}from"@/components/business/business-switcher";import{useBusiness}from"@/contexts/business-context";import{cn}from"@/lib/utils";const links=[{href:"/documents",label:"Overview",icon:LayoutDashboard},{href:"/documents/list",label:"Documents",icon:Files},{href:"/documents/upload",label:"Upload",icon:Upload},{href:"/documents/review",label:"Pending review",icon:FileSearch}];export function DocumentShell({children}:{children:React.ReactNode}){const p=usePathname(),{businesses}=useBusiness();return <BusinessRouteShell><div className="space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-primary">Intelligent Documents</p><h1 className="text-2xl font-semibold tracking-tight">Document workspace</h1><p className="mt-1 text-sm text-muted-foreground">Capture, extract, review, and automate verified business documents.</p></div>{businesses.length>1&&<div className="w-full sm:w-72"><BusinessSwitcher/></div>}</header><nav aria-label="Document sections" className="flex gap-1 overflow-x-auto border-b">{links.map(({href,label,icon:Icon})=>{const on=href==="/documents"?p===href:p.startsWith(href);return <Link key={href} href={href} className={cn("flex items-center gap-2 whitespace-nowrap border-b-2 px-3 py-2.5 text-sm font-medium",on?"border-primary":"border-transparent text-muted-foreground")}><Icon className="size-4"/>{label}</Link>})}</nav>{children}</div></BusinessRouteShell>}
