"use client"; import { zodResolver } from "@hookform/resolvers/zod"; import { useForm } from "react-hook-form"; import { useRouter } from "next/navigation"; import { toast } from "sonner"; import { useBusiness } from "@/contexts/business-context"; import { usePurchaseMutations, useSupplier } from "@/features/purchases/hooks/use-purchases"; import { supplierSchema, type SupplierFormValues } from "@/features/purchases/schemas/purchase-schemas"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label"; import { PurchasesLoading } from "./purchases-states";
export function SupplierForm({ id }: { id?: string }) { const existing = useSupplier(id ?? ""); if (id && existing.isLoading) return <PurchasesLoading rows={4} />; const initial = existing.data ? { name: existing.data.name, email: existing.data.email ?? "", phone: existing.data.phone ?? "", gstin: existing.data.gstin ?? "", pan: existing.data.pan ?? "", address: existing.data.address ?? "", payment_terms: existing.data.payment_terms ?? "", is_active: existing.data.is_active } : undefined; return <SupplierFormInner id={id} initial={initial} />; }
function SupplierFormInner({ id, initial }: { id?: string; initial?: SupplierFormValues }) { const { activeBusinessId } = useBusiness(); const mutations = usePurchaseMutations(); const router = useRouter(); const { register, handleSubmit, formState: { errors, isSubmitting }, setError } = useForm<SupplierFormValues>({ resolver: zodResolver(supplierSchema), defaultValues: initial ?? { name: "", email: "", phone: "", gstin: "", pan: "", address: "", payment_terms: "", is_active: true } }); const submit = handleSubmit(async (values) => { try { const parsed = supplierSchema.parse(values); const result = id ? await mutations.updateSupplier.mutateAsync({ id, input: parsed }) : await mutations.createSupplier.mutateAsync({ businessId: activeBusinessId!, input: parsed }); toast.success(id ? "Supplier updated" : "Supplier created"); router.push(`/purchases/suppliers/${result.id}`); } catch (error) { setError("root", { message: error instanceof Error ? error.message : "Supplier could not be saved." }); } }); return <form onSubmit={submit} noValidate className="mx-auto max-w-3xl space-y-5 rounded-xl border bg-card p-5"><div><h2 className="text-lg font-semibold">{id ? "Edit supplier" : "New supplier"}</h2><p className="text-sm text-muted-foreground">Only fields supported by the supplier API are included.</p></div>{errors.root?.message && <p role="alert" className="text-sm text-destructive">{errors.root.message}</p>}<div className="grid gap-4 sm:grid-cols-2"><Field label="Supplier name" error={errors.name?.message}><Input {...register("name")} /></Field><Field label="Email" error={errors.email?.message}><Input type="email" {...register("email")} /></Field><Field label="Phone" error={errors.phone?.message}><Input {...register("phone")} /></Field><Field label="Payment terms" error={errors.payment_terms?.message}><Input {...register("payment_terms")} placeholder="Net 30" /></Field><Field label="GSTIN" error={errors.gstin?.message}><Input {...register("gstin")} /></Field><Field label="PAN" error={errors.pan?.message}><Input {...register("pan")} /></Field></div><Field label="Address" error={errors.address?.message}><textarea {...register("address")} className="min-h-24 w-full rounded-lg border bg-background p-3 text-sm" /></Field><label className="flex items-center gap-2 text-sm"><input type="checkbox" {...register("is_active")} />Active supplier</label><div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={() => router.back()}>Cancel</Button><Button type="submit" disabled={isSubmitting}>{isSubmitting ? "Saving…" : "Save supplier"}</Button></div></form>; }
function Field({ label, error, children }: { label: string; error?: string; children: React.ReactNode }) { return <label className="space-y-2"><Label>{label}</Label>{children}{error && <span className="block text-xs text-destructive">{error}</span>}</label>; }
