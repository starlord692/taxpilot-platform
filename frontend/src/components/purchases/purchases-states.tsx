import { AlertCircle, Database } from "lucide-react"; import { Button } from "@/components/ui/button"; import { Skeleton } from "@/components/ui/skeleton";
export function PurchasesLoading({ rows = 5 }: { rows?: number }) { return <div aria-label="Loading purchases" aria-busy="true" className="space-y-3">{Array.from({ length: rows }, (_, index) => <Skeleton key={index} className="h-16 rounded-xl" />)}</div>; }
export function PurchasesEmpty({ title, description }: { title: string; description: string }) { return <div className="rounded-xl border border-dashed p-10 text-center"><Database className="mx-auto size-8 text-muted-foreground" /><h2 className="mt-4 font-semibold">{title}</h2><p className="mx-auto mt-1 max-w-md text-sm text-muted-foreground">{description}</p></div>; }
export function PurchasesError({ retry }: { retry: () => void }) { return <div role="alert" className="rounded-xl border border-destructive/30 bg-destructive/5 p-6 text-center"><AlertCircle className="mx-auto size-7 text-destructive" /><h2 className="mt-3 font-semibold">Purchases data unavailable</h2><p className="mt-1 text-sm text-muted-foreground">The request could not be completed. Existing records were not changed.</p><Button variant="outline" className="mt-4" onClick={retry}>Try again</Button></div>; }
