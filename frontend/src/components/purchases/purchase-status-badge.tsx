import { cn } from "@/lib/utils"; import type { PurchaseStatus } from "@/features/purchases/types/purchases.types"; import { titleCase } from "@/features/purchases/utils/purchase-utils";
export function PurchaseStatusBadge({ status }: { status: PurchaseStatus }) { const tone = status === "paid" ? "bg-emerald-500/10 text-emerald-700 dark:text-emerald-400" : status === "cancelled" ? "bg-red-500/10 text-red-700 dark:text-red-400" : status === "approved" ? "bg-blue-500/10 text-blue-700 dark:text-blue-400" : status === "received" ? "bg-violet-500/10 text-violet-700 dark:text-violet-400" : "bg-muted text-muted-foreground"; return <span className={cn("inline-flex rounded-full px-2.5 py-1 text-xs font-medium", tone)}>{titleCase(status)}</span>; }
