"use client";
import { Building2 } from "lucide-react";
import { useBusiness } from "@/contexts/business-context";
import { Skeleton } from "@/components/ui/skeleton";
export function BusinessSwitcher() { const { businesses, activeBusiness, activeBusinessId, status, selectBusiness } = useBusiness(); if (status === "initializing") return <Skeleton className="h-12 w-full rounded-lg" />; if (status === "unavailable") return <div className="rounded-lg border border-destructive/20 bg-destructive/5 p-3 text-xs text-destructive">Business context unavailable</div>; if (status === "no-business") return <div className="rounded-lg border p-3 text-xs text-muted-foreground">No business available</div>; if (businesses.length === 1 && activeBusiness) return <div className="flex items-center gap-3 rounded-lg border bg-background p-2.5"><span className="grid size-8 place-items-center rounded-lg bg-primary/10 text-primary"><Building2 className="size-4" /></span><div className="min-w-0"><p className="truncate text-xs font-medium">{activeBusiness.trade_name || activeBusiness.legal_name}</p><p className="text-[11px] text-muted-foreground">Active business</p></div></div>; return <label className="block"><span className="mb-1.5 block text-[11px] font-medium text-muted-foreground">Active business</span><select aria-label="Active business" value={activeBusinessId ?? ""} onChange={(event) => void selectBusiness(event.target.value)} className="h-10 w-full rounded-lg border bg-background px-3 text-xs outline-none focus:ring-2 focus:ring-ring"><option value="" disabled>Choose a business</option>{businesses.map((business) => <option key={business.id} value={business.id}>{business.trade_name || business.legal_name}</option>)}</select><span className="sr-only" aria-live="polite">{activeBusiness ? `${activeBusiness.trade_name || activeBusiness.legal_name} selected` : "No business selected"}</span></label>; }
