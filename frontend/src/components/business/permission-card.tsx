import { KeyRound } from "lucide-react";
import { permissionModules } from "@/features/business/utils/permission-summary";
export function PermissionCard({ roles = [], permissions = [] }: { roles?: string[]; permissions?: string[] }) { const modules = permissionModules(permissions); return <section className="rounded-xl border bg-card p-5"><h2 className="flex items-center gap-2 text-sm font-semibold"><KeyRound className="size-4 text-muted-foreground" />Permission Summary</h2><div className="mt-5 grid gap-5 sm:grid-cols-3"><div><p className="text-xs text-muted-foreground">Current account roles</p><div className="mt-2 flex flex-wrap gap-1.5">{roles.length ? roles.map((role) => <span key={role} className="rounded-md bg-muted px-2 py-1 text-xs capitalize">{role}</span>) : <span className="text-xs text-muted-foreground">Unavailable for this session</span>}</div></div><div><p className="text-xs text-muted-foreground">Accessible modules</p><div className="mt-2 flex flex-wrap gap-1.5">{modules.length ? modules.map((module) => <span key={module} className="rounded-md bg-muted px-2 py-1 text-xs capitalize">{module}</span>) : <span className="text-xs text-muted-foreground">No module metadata available</span>}</div></div><div><p className="text-xs text-muted-foreground">Permission level</p><p className="mt-2 text-xs text-muted-foreground">Unavailable from current API</p></div></div>{permissions.length > 0 && <div className="mt-5 border-t pt-4"><p className="text-xs text-muted-foreground">Granted permissions</p><ul className="mt-2 grid gap-1.5 sm:grid-cols-2">{permissions.map((permission) => <li key={permission} className="font-mono text-xs">{permission}</li>)}</ul></div>}</section>; }
