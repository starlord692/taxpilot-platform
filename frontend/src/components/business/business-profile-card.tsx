import type { BusinessSummary, GstRegistration } from "@/features/business/types/business.types";
function Row({ label, value, unavailable }: { label: string; value?: string | null; unavailable?: boolean }) { return <div className="border-b py-3 last:border-0 sm:grid sm:grid-cols-[180px_1fr]"><dt className="text-xs text-muted-foreground">{label}</dt><dd className="mt-1 text-sm sm:mt-0">{value || <span className="text-muted-foreground">— {unavailable ? "Unavailable from current API" : "Not provided"}</span>}</dd></div>; }
export function BusinessProfileCard({ business, gst }: { business: BusinessSummary; gst?: GstRegistration }) { return <section className="rounded-xl border bg-card p-5"><h2 className="text-sm font-semibold">Business Profile</h2><dl className="mt-3"><Row label="Business name" value={business.trade_name} /><Row label="Legal name" value={business.legal_name} /><Row label="Business code" unavailable /><Row label="GSTIN" value={gst?.gstin} /><Row label="PAN" unavailable /><Row label="Business type" value={business.business_type.replaceAll("_", " ")} /><Row label="Address" unavailable /><Row label="Country" unavailable /><Row label="State code" value={gst?.state_code} /><Row label="Currency" unavailable /><Row label="Timezone" unavailable /><Row label="Financial year" unavailable /></dl></section>; }
