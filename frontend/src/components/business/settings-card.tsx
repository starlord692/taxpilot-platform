import { Settings2 } from "lucide-react";
const settings = ["Currency", "Date format", "Number format", "Timezone", "Invoice prefix", "Financial year"];
export function SettingsCard() { return <section className="rounded-xl border bg-card p-5"><h2 className="flex items-center gap-2 text-sm font-semibold"><Settings2 className="size-4 text-muted-foreground" />Business Settings</h2><p className="mt-2 text-xs leading-5 text-muted-foreground">Settings cannot be loaded or edited because the accessible business response does not provide the business code required by the detail API.</p><dl className="mt-4 divide-y">{settings.map((setting) => <div key={setting} className="flex items-center gap-4 py-3"><dt className="text-xs text-muted-foreground">{setting}</dt><dd className="ml-auto text-xs text-muted-foreground">Unavailable from current API</dd></div>)}</dl></section>; }
