import type { GstRegistration } from "@/features/business/types/business.types";
import { EmptyState } from "@/components/dashboard/empty-state";
import { StatusBadge } from "./status-badge";
export function TaxProfileCard({ registrations }: { registrations: GstRegistration[] }) { if (!registrations.length) return <section className="rounded-xl border bg-card"><EmptyState title="No GST registration found" description="No GST registration was returned for this business." /></section>; return <section className="rounded-xl border bg-card p-5"><h2 className="text-sm font-semibold">Tax Profile</h2><div className="mt-4 space-y-3">{registrations.map((item) => <article key={item.id} className="rounded-lg border p-4"><div className="flex flex-wrap items-center gap-2"><p className="font-mono text-sm font-medium">{item.gstin}</p><StatusBadge label={item.is_active ? "active" : "inactive"} positive={item.is_active} /></div><dl className="mt-4 grid gap-3 text-sm sm:grid-cols-2"><div><dt className="text-xs text-muted-foreground">Registration type</dt><dd className="mt-1 capitalize">{item.registration_type.replaceAll("_", " ")}</dd></div><div><dt className="text-xs text-muted-foreground">Composition scheme</dt><dd className="mt-1">{item.is_composition_scheme ? "Yes" : "No"}</dd></div><div><dt className="text-xs text-muted-foreground">State code</dt><dd className="mt-1">{item.state_code}</dd></div><div><dt className="text-xs text-muted-foreground">Filing frequency</dt><dd className="mt-1 text-muted-foreground">Unavailable from current API</dd></div></dl></article>)}</div></section>; }
