"use client";import{useAuth}from"@/contexts/auth-context";import{UserAvatar}from"@/components/layout/user-avatar";import{SettingsCard,Fact}from"./settings-card";import{Unavailable}from"@/components/inventory/inventory-states";export function UserProfileSettings(){const{user}=useAuth();return <div className="space-y-5"><SettingsCard title="User profile" description="Read-only identity fields from /identity/me.">{user?<div className="flex flex-col gap-6 sm:flex-row"><UserAvatar name={user.display_name} email={user.email} className="size-16 text-lg"/><dl className="grid flex-1 gap-5 sm:grid-cols-2"><Fact label="Display name" value={user.display_name}/><Fact label="Email" value={user.email}/><Fact label="First name" value={user.first_name}/><Fact label="Last name" value={user.last_name}/><Fact label="Account status" value={user.status}/><Fact label="User ID" value={<span className="font-mono">{user.id}</span>}/></dl></div>:<p className="text-sm text-muted-foreground">User profile is unavailable.</p>}</SettingsCard><Unavailable title="Profile editing unavailable" description="The backend exposes current-user information but no current-user profile update or avatar upload endpoint."/></div>}
