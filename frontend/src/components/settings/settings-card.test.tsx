import{render,screen}from"@testing-library/react";import{describe,expect,it}from"vitest";import{SettingsCard,Fact}from"./settings-card";describe("settings shared components",()=>{it("renders semantic section content",()=>{render(<SettingsCard title="Account" description="Verified account information"><dl><Fact label="Status" value="active"/></dl></SettingsCard>);expect(screen.getByRole("heading",{name:"Account"})).toBeInTheDocument();expect(screen.getByText("Verified account information")).toBeInTheDocument();expect(screen.getByText("Status")).toBeInTheDocument();expect(screen.getByText("active")).toBeInTheDocument()})});
