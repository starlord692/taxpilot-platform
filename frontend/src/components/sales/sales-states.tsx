import { AlertCircle, Database, Search } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Skeleton } from "@/components/ui/skeleton";

export function SalesLoading({ rows = 5 }: { rows?: number }) { return <div aria-label="Loading sales data" aria-busy="true" className="space-y-3">{Array.from({ length: rows }, (_, i) => <Skeleton key={i} className="h-16 w-full rounded-xl" />)}</div>; }
export function SalesEmpty({ title, description }: { title: string; description: string }) { return <div className="rounded-xl border border-dashed p-10 text-center"><Database className="mx-auto size-8 text-muted-foreground" /><h2 className="mt-4 font-semibold">{title}</h2><p className="mx-auto mt-1 max-w-md text-sm text-muted-foreground">{description}</p></div>; }
export function SalesError({ retry }: { retry: () => void }) { return <div role="alert" className="rounded-xl border border-destructive/30 bg-destructive/5 p-6 text-center"><AlertCircle className="mx-auto size-7 text-destructive" /><h2 className="mt-3 font-semibold">Sales data unavailable</h2><p className="mt-1 text-sm text-muted-foreground">The request could not be completed. Your existing data was not changed.</p><Button variant="outline" className="mt-4" onClick={retry}>Try again</Button></div>; }
export function UnsupportedTools() { return <div className="rounded-xl border bg-muted/25 p-4"><div className="flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-2.5 size-4 text-muted-foreground" /><input disabled aria-describedby="invoice-tools-help" aria-label="Invoice search unavailable" placeholder="Invoice number search unavailable" className="h-9 w-full rounded-md border bg-background pl-9 pr-3 text-sm disabled:opacity-70" /></div><Button disabled variant="outline">Status and customer filters unavailable</Button><Button disabled variant="outline">Date filter unavailable</Button></div><p id="invoice-tools-help" className="mt-2 text-xs text-muted-foreground">The canonical API supports newest-first pagination only. No undocumented search or filter parameters are sent.</p></div>; }
