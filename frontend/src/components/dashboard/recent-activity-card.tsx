import { AlertCircle, type LucideIcon } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Skeleton } from "@/components/ui/skeleton";
import { EmptyState } from "./empty-state";
export interface ActivityItem { id: string; title: string; subtitle: string; amount: string; status: string; }
export function RecentActivityCard({ title, icon: Icon, items, isLoading, isError, unavailableReason, onRetry }: { title: string; icon: LucideIcon; items?: ActivityItem[]; isLoading?: boolean; isError?: boolean; unavailableReason?: string; onRetry?: () => void }) { return <section className="overflow-hidden rounded-xl border bg-card" aria-labelledby={`activity-${title.replaceAll(" ", "-").toLowerCase()}`}><header className="flex items-center gap-2 border-b px-5 py-4"><Icon className="size-4 text-muted-foreground" /><h2 id={`activity-${title.replaceAll(" ", "-").toLowerCase()}`} className="text-sm font-semibold">{title}</h2></header>{isLoading ? <div className="space-y-4 p-5" aria-busy="true">{Array.from({ length: 3 }).map((_, index) => <div className="flex gap-3" key={index}><Skeleton className="size-8 rounded-lg" /><div className="flex-1"><Skeleton className="h-3 w-2/3" /><Skeleton className="mt-2 h-3 w-1/3" /></div></div>)}</div> : isError ? <div className="p-6 text-center" role="alert"><AlertCircle className="mx-auto size-5 text-destructive" /><p className="mt-2 text-sm font-medium">Activity could not be loaded</p><Button variant="outline" size="sm" className="mt-4" onClick={onRetry}>Try again</Button></div> : unavailableReason ? <EmptyState compact title="Recent activity unavailable" description={unavailableReason} /> : !items?.length ? <EmptyState compact title={`No ${title.toLowerCase()}`} description="New records will appear here when they are available." /> : <ul className="divide-y">{items.map((item) => <li key={item.id} className="flex items-center gap-3 px-5 py-3.5"><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{item.title}</p><p className="mt-0.5 truncate text-xs text-muted-foreground">{item.subtitle}</p></div><div className="text-right"><p className="text-sm font-medium tabular-nums">{item.amount}</p><p className="mt-0.5 capitalize text-[11px] text-muted-foreground">{item.status}</p></div></li>)}</ul>}</section>; }
