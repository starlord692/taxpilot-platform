import { Sparkles } from "lucide-react";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Skeleton } from "@/components/ui/skeleton";

export function WorkspaceInsightsCard({ pendingReviews, loading, error }: { pendingReviews?: number; loading?: boolean; error?: boolean }) {
  return <Card aria-labelledby="workspace-insights-title"><CardHeader><div className="flex items-start justify-between gap-3"><div><CardTitle id="workspace-insights-title">AI insights</CardTitle><CardDescription>Verified signals and future AI guidance.</CardDescription></div><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-primary/10 text-primary"><Sparkles className="size-5" aria-hidden="true" /></span></div></CardHeader><CardContent>{loading ? <div aria-label="Loading insights" aria-busy="true"><Skeleton className="h-16 rounded-xl" /></div> : error ? <p role="alert" className="rounded-xl border border-destructive/20 bg-destructive/5 p-4 text-sm text-destructive">Document review signals could not be loaded.</p> : pendingReviews ? <div className="rounded-xl border bg-primary/[0.04] p-4"><p className="text-sm font-medium">{pendingReviews} {pendingReviews === 1 ? "document requires" : "documents require"} review</p><p className="mt-1 text-xs leading-5 text-muted-foreground">This is a verified workflow signal from the document review service—not an AI-generated financial claim.</p></div> : <p className="rounded-xl border border-dashed p-4 text-sm text-muted-foreground">No verified AI insight source is currently available. Insights will appear here only when backed by existing platform data.</p>}</CardContent></Card>;
}
