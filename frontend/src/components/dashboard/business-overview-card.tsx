import { Building2, CircleCheck, Landmark } from "lucide-react";
import type { BusinessSummary, GstRegistration } from "@/features/dashboard/types/dashboard.types";
export function BusinessOverviewCard({ business, gst, gstLoading, gstError }: { business: BusinessSummary; gst?: GstRegistration; gstLoading: boolean; gstError?: boolean }) { const name = business.trade_name || business.legal_name; return <section aria-labelledby="business-overview-title" className="rounded-xl border bg-card p-5"><div className="grid gap-5 sm:grid-cols-3"><div><p id="business-overview-title" className="flex items-center gap-2 text-xs font-medium uppercase tracking-wider text-muted-foreground"><Building2 className="size-4" />Business</p><p className="mt-2 truncate text-sm font-semibold" title={name}>{name}</p><p className="mt-1 truncate text-xs text-muted-foreground" title={business.legal_name}>{business.legal_name}</p></div><div><p className="flex items-center gap-2 text-xs font-medium uppercase tracking-wider text-muted-foreground"><Landmark className="size-4" />GST status</p><p className="mt-2 text-sm font-semibold">{gstLoading ? "Checking…" : gstError ? "Unavailable" : gst?.is_active ? "GST registered" : "Not registered"}</p>{gst?.gstin && <p className="mt-1 text-xs text-muted-foreground">GSTIN {gst.gstin}</p>}</div><div><p className="flex items-center gap-2 text-xs font-medium uppercase tracking-wider text-muted-foreground"><CircleCheck className="size-4" />Account status</p><p className="mt-2 capitalize text-sm font-semibold">{business.status}</p><p className="mt-1 text-xs text-muted-foreground">Business access verified</p></div></div></section>; }
