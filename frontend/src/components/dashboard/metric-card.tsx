import type { LucideIcon } from "lucide-react";
export function MetricCard({ label, icon: Icon, description = "Aggregate data is not available from the current API." }: { label: string; icon: LucideIcon; description?: string }) { return <article className="min-w-0 rounded-xl border bg-card p-4"><div className="flex items-center justify-between"><p className="truncate text-xs font-medium text-muted-foreground">{label}</p><Icon className="size-4 text-muted-foreground" aria-hidden="true" /></div><p className="mt-4 text-2xl font-semibold tabular-nums" aria-label={`${label}: data unavailable`}>—</p><p className="mt-1 text-[11px] leading-4 text-muted-foreground">{description}</p></article>; }
