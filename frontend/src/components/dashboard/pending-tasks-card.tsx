import Link from "next/link";
import { CheckCircle2, FileCheck2 } from "lucide-react";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Skeleton } from "@/components/ui/skeleton";

export function PendingTasksCard({ pendingReviews, loading, error }: { pendingReviews?: number; loading?: boolean; error?: boolean }) {
  return <Card aria-labelledby="pending-tasks-title"><CardHeader><CardTitle id="pending-tasks-title">Pending tasks</CardTitle><CardDescription>Actions supported by verified workflow state.</CardDescription></CardHeader><CardContent>{loading ? <div className="space-y-2" aria-label="Loading tasks" aria-busy="true"><Skeleton className="h-14 rounded-xl" /><Skeleton className="h-14 rounded-xl" /></div> : error ? <p role="alert" className="rounded-xl border border-destructive/20 bg-destructive/5 p-4 text-sm text-destructive">Pending tasks could not be loaded.</p> : pendingReviews ? <Link href="/documents/review" className="flex min-h-14 items-center gap-3 rounded-xl border bg-background p-3 outline-none hover:bg-accent focus-visible:ring-2 focus-visible:ring-ring"><span className="grid size-9 place-items-center rounded-lg bg-amber-500/10 text-amber-700 dark:text-amber-400"><FileCheck2 className="size-4" /></span><div className="min-w-0"><p className="text-sm font-medium">Review OCR documents</p><p className="text-xs text-muted-foreground">{pendingReviews} pending {pendingReviews === 1 ? "review" : "reviews"}</p></div><span className="ml-auto" aria-hidden="true">→</span></Link> : <div className="flex items-center gap-3 rounded-xl border border-dashed p-4"><CheckCircle2 className="size-5 text-emerald-600" aria-hidden="true" /><div><p className="text-sm font-medium">No verified tasks pending</p><p className="mt-0.5 text-xs text-muted-foreground">New actionable items will appear when supported by platform workflows.</p></div></div>}</CardContent></Card>;
}
