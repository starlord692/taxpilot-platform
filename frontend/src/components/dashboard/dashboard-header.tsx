import { CalendarDays, Plus } from "lucide-react";
import { Button } from "@/components/ui/button";
import { formatDate } from "@/features/dashboard/utils/dashboard-formatters";
export function DashboardHeader({ firstName }: { firstName?: string }) { return <header className="flex flex-col gap-5 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-sm font-medium text-primary">Overview</p><h1 className="mt-1 text-2xl font-semibold tracking-[-0.025em] sm:text-3xl">Welcome back{firstName ? `, ${firstName}` : ""}</h1><p className="mt-2 text-sm text-muted-foreground">Here is what is available across your business today.</p></div><div className="flex flex-wrap items-center gap-2"><div className="flex h-9 items-center gap-2 rounded-md border bg-card px-3 text-xs text-muted-foreground"><CalendarDays className="size-4" aria-hidden="true" /><time dateTime={new Date().toISOString().slice(0, 10)}>{formatDate(new Date())}</time></div><Button disabled title="ERP actions are coming soon"><Plus />Quick action</Button></div></header>; }
