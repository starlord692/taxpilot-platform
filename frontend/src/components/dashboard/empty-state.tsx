import { Inbox } from "lucide-react";
export function EmptyState({ title, description, compact = false }: { title: string; description: string; compact?: boolean }) { return <div className={compact ? "py-6 text-center" : "grid min-h-44 place-items-center px-5 py-8 text-center"}><div><span className="mx-auto grid size-9 place-items-center rounded-lg bg-muted text-muted-foreground"><Inbox className="size-4" aria-hidden="true" /></span><p className="mt-3 text-sm font-medium">{title}</p><p className="mx-auto mt-1 max-w-sm text-xs leading-5 text-muted-foreground">{description}</p></div></div>; }
