"use client";
import { LogOut, UserRound } from "lucide-react";
import { useAuth } from "@/contexts/auth-context";
import { UserAvatar } from "./user-avatar";
export function UserMenu() { const { user, logout } = useAuth(); if (!user) return null; return <details className="group relative"><summary className="list-none cursor-pointer rounded-full outline-none focus-visible:ring-2 focus-visible:ring-ring"><UserAvatar name={user.display_name} email={user.email} /></summary><div className="absolute right-0 top-11 w-64 overflow-hidden rounded-xl border bg-popover text-popover-foreground shadow-xl"><div className="flex gap-3 border-b p-4"><UserAvatar name={user.display_name} email={user.email} className="size-9" /><div className="min-w-0"><p className="truncate text-sm font-medium">{user.display_name}</p><p className="truncate text-xs text-muted-foreground">{user.email}</p></div></div><div className="p-1.5"><div className="flex items-center gap-2 rounded-lg px-2.5 py-2 text-xs text-muted-foreground"><UserRound className="size-4" />{user.status === "active" ? "Active account" : user.status}</div><button type="button" onClick={() => logout()} className="flex w-full items-center gap-2 rounded-lg px-2.5 py-2 text-left text-sm hover:bg-accent focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"><LogOut className="size-4" />Sign out</button></div></div></details>; }
