"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { X } from "lucide-react";
import { navigation } from "@/config/navigation";
import { cn } from "@/lib/utils";
import { BusinessSwitcher } from "@/components/business/business-switcher";
export function Sidebar({ mobileOpen, onClose }: { mobileOpen: boolean; onClose: () => void }) { const pathname = usePathname(); return <><button aria-label="Close navigation" className={cn("fixed inset-0 z-40 bg-black/30 backdrop-blur-sm lg:hidden", mobileOpen ? "block" : "hidden")} onClick={onClose} /><aside className={cn("fixed inset-y-0 left-0 z-50 flex w-64 flex-col border-r bg-card transition-transform duration-200 lg:translate-x-0", mobileOpen ? "translate-x-0" : "-translate-x-full")}><div className="flex h-14 items-center border-b px-5"><Link href="/" className="flex items-center gap-2 font-semibold tracking-tight" onClick={onClose}><span className="grid size-7 place-items-center rounded-lg bg-primary text-xs font-bold text-primary-foreground">T</span>TaxPilot</Link><button className="ml-auto rounded-md p-1.5 text-muted-foreground hover:bg-accent lg:hidden" onClick={onClose} aria-label="Close navigation"><X className="size-4" /></button></div><div className="border-b p-3"><BusinessSwitcher /></div><nav className="flex-1 space-y-1 p-3" aria-label="Main navigation">{navigation.map(({ label, href, icon: Icon, disabled }) => { const active = pathname === href || (href !== "/" && pathname.startsWith(href)); return <Link key={label} href={disabled ? "#" : href} aria-disabled={disabled} onClick={disabled ? undefined : onClose} className={cn("flex items-center gap-3 rounded-lg px-3 py-2 text-sm font-medium transition-colors", active ? "bg-accent text-accent-foreground" : "text-muted-foreground hover:bg-accent/60 hover:text-foreground", disabled && "pointer-events-none opacity-40")}><Icon className="size-4" />{label}{disabled && <span className="ml-auto text-[10px] font-normal uppercase tracking-wider">Soon</span>}</Link>; })}</nav><div className="border-t p-4"><p className="text-xs font-medium">Business workspace</p><p className="mt-1 text-xs text-muted-foreground">Frontend v0.1.0</p></div></aside></>; }
